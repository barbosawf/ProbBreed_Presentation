# --- Quarto ---
/.quarto/
*_cache/
*_files/
/.quarto_ipynb_checkpoints/

# --- Rendered/exported output (regenerate with `quarto render`) ---
*.html
*.pdf
*.docx
*.pptx

# --- R / RStudio session & environment files ---
.Rproj.user/
.Rhistory
.RData
.Ruserdata
.Rapp.history
*.Rcheck/
.httr-oauth
renv/library/
renv/staging/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*~
.vscode/

# --- Local AI-assistant tooling config (not part of the Quarto project) ---
.claude/

# --- Reference material and scratch assets kept outside the repo ---
# Source papers/templates used as research material while writing the
# talk -- not read by any code, and the logo images actually used by the
# slides already live (with clean names) under images/. Keeping this out
# keeps the repo light per the project owner's request.
/References/
images/download.png

**/*.quarto_ipynb

---
title: "ProbBreed"
subtitle: "A Probabilistic Framework for Cultivar Recommendation in Multi-Environment Trials"
author:
  - name: "Wagner Faria Barbosa"
    affiliation: "Post-doctoral researcher"
  - name: "Rhoda Ayanfeoluwa Ojo"
    affiliation: "Master student"
date: "2026-08-21"
date-format: long
format:
  revealjs:
    theme: [simple, theme/probbreed.scss]
    highlight-style: kate
    slide-number: true
    show-slide-number: all
    scrollable: true   # safety net: lets any unexpectedly tall slide scroll
                        # instead of clipping content off the bottom edge
    progress: true
    history: true
    chalkboard: true
    preview-links: auto
    transition: slide
    background-transition: fade
    code-line-numbers: false
    footer: "ProbBreed Journal Club · Quantitative Genetics and Statistical Learning Lab"
    # ------------------------------------------------------------------
    # MULTIPLEX ("audience follows the speaker's slides live")
    # ------------------------------------------------------------------
    # Setting this single flag to `true` is enough. Quarto will silently:
    #   1) Call the public Reveal Multiplex relay server
    #      (https://multiplex.up.railway.app/) at render time and have
    #      it generate a one-time token pair (an `id` and a `secret`)
    #      for this deck -- you do NOT need to visit any website or
    #      generate anything by hand.
    #   2) Bake that token pair into TWO separate HTML outputs when you
    #      run `quarto render presentation_probbreed.qmd`:
    #        - presentation_probbreed.html          <- contains only the
    #          public `id`. This is the file you SHARE with the audience
    #          (upload it somewhere reachable, e.g. GitHub Pages, a
    #          shared drive with a live web server, or the lab's local
    #          network). They just open it and watch -- no controls.
    #        - presentation_probbreed-speaker.html   <- contains the
    #          `secret` as well. This is the file YOU open and drive
    #          from the podium. Keep it local; never publish or share
    #          it, since the secret is what grants control of the deck.
    #   3) Keep every viewer's browser in sync with yours in real time:
    #      whenever you press "next slide" on the speaker copy, every
    #      open audience copy advances automatically over a WebSocket
    #      relay -- no clicking required on their end.
    #
    # Practical distribution steps for our lab meeting:
    #   a) Render the deck: `quarto render presentation_probbreed.qmd`
    #   b) Host `presentation_probbreed.html` somewhere reachable by
    #      everyone's laptop/phone on the day of the talk (e.g., a
    #      `quarto preview` session on the shared lab Wi-Fi, a GitHub
    #      Pages URL, or a quick `python3 -m http.server` on your
    #      machine) and share that link/QR code with the audience.
    #   c) Open `presentation_probbreed-speaker.html` yourself and
    #      present as usual -- every "next slide" you trigger mirrors
    #      automatically on everyone else's screen.
    #   d) If you would rather run your own relay server instead of the
    #      default public one, or pin a fixed id/secret across re-
    #      renders, see: https://quarto.org/docs/presentations/revealjs/presenting.html#multiplex
    multiplex: true
execute:
  echo: true
  # NOTE: `eval` is intentionally left at its default (true) at the
  # document level. The `bayes_met()` snippets below are plain ```r
  # fences (not executable ```{r} chunks), so they are always displayed
  # as syntax only and never run -- the fully executable, reproducible
  # version of every analysis step lives in the companion document
  # `hands_on_tutorial.qmd`. Setting `eval: false` here would also
  # (harmfully) disable Quarto's Mermaid diagram engine, since ```{mermaid}
  # blocks are executable cells too.
---

::: {.title-logos}
![](images/logo-ufv.png){.title-logo}
![](images/logo-idata.jpeg){.title-logo}
![](images/logo-qgsl.jpeg){.title-logo}
:::

<div class="notes">
Welcome everyone, thank them for coming to the lab meeting. Introduce yourself and Rhoda, and mention this is a joint presentation. [Wait for the room to settle before advancing.] One-sentence framing: "Today we're going to walk through ProbBreed, a package Kaio and collaborators built for turning multi-environment trial data into risk-aware cultivar recommendations -- first the theory, then a hands-on session where everyone runs the code with us."
</div>

```{=html}
<script>
// Quarto's revealjs writer puts any content placed before the first
// heading into a brand-new (empty-looking) slide of its own -- it does
// NOT merge into the auto-generated #title-slide block. This relocates
// the .title-logos row (and this slide's speaker notes) into the real
// title slide right after they're parsed, then removes the now-empty
// stray slide so it doesn't show up as a blank slide in navigation.
// IMPORTANT: this script tag must come AFTER both .title-logos and
// .notes in the source -- inline <script> runs synchronously mid-parse,
// so anything placed after it doesn't exist in the DOM yet when it runs.
(function () {
  var strayHost = document.currentScript.parentElement;
  var logos = strayHost.querySelector('.title-logos');
  var notes = strayHost.querySelector('.notes');
  var titleSlide = document.getElementById('title-slide');
  if (titleSlide) {
    if (logos) titleSlide.appendChild(logos);
    if (notes) titleSlide.appendChild(notes);
  }
  strayHost.remove();
})();
</script>
```

## Why cultivar recommendation is risky

::: {.incremental}
- Breeding programs test dozens of genotypes across many locations and years before recommending one to growers
- **Genotype-by-environment interaction (GEI)** means a genotype's rank can change from site to site
- Classical tools (BLUEs/BLUPs, AMMI/GGE biplots) tell us *which genotype ranks higher on average* — mostly descriptive
- They rarely answer what breeders actually need to tell growers:
  > "How **likely** is this genotype to still be a good choice next year, somewhere new?"
:::

<div class="notes">
Open with the core motivating problem. [Click through the bullets one at a time, pause after each.] Start concrete: "Imagine you've run trials at 16 locations for 3 years. You have a ranking. But rankings hide something important -- genotype-by-environment interaction, GEI, means the #1 genotype at one site might be #20 somewhere else." Land on the quoted question -- read it out loud, slowly, it's the thesis of the whole talk. Emphasize that AMMI/GGE biplots are great for *exploring* GEI visually but don't give you a number you can act on.
</div>

## From ranking to risk

::: {.callout-tip}
## `ProbBreed`'s reframe
Instead of *"which genotype has the highest mean?"*, ask:

**"What is the probability that this genotype belongs to the top performers — and how confident can we be in that recommendation?"**
:::

This shift — from a point estimate to a **probability of success** — is what makes the framework directly usable in a breeder's decision routine.

`ProbBreed` implements the Bayesian method of **Dias *et al.* (2022, *TAG*)** as an `R` package, described in **Chaves *et al.* (2024, *G3*)**.

<div class="notes">
[Point at the callout box -- this is the single most important idea in the talk, say it slowly.] "This is the reframe: instead of a single best-guess ranking, we ask for a probability." Explain why that matters practically: a probability is something a breeder can weigh against cost, time, and risk tolerance, the same way they already think about selection intensity. Then bridge to the citation: this isn't a new statistical idea we invented for the talk -- it's a peer-reviewed method (Dias et al. 2022) that Saulo Chaves and colleagues packaged into easy-to-use R functions (Chaves et al. 2024), which is what we're demonstrating today.
</div>

## Why Bayesian?

| | Frequentist ranking | Bayesian probability |
|---|---|---|
| Output | Point estimate / rank | Full posterior distribution |
| Uncertainty | Awkward for ratios & rankings | Propagated to any derived quantity |
| "Risk of recommendation" | Not directly available | Directly computable as a probability |
| Interpretation | "Genotype A's BLUP is higher" | "Genotype A has an 87% chance of being top-tier" |

- Models are fit with **Stan** (`rstan`), using the self-tuning **No-U-Turn Sampler**
- Priors are weakly informative and data-driven: $\phi = \max(y) \times 10$
- Payoff: the **entire posterior** of every genotypic effect feeds directly into probability calculations

<div class="notes">
[Give the table a moment to be read before you talk over it.] Walk row by row: "Output" -- frequentist gives you one number per genotype, Bayesian gives you a whole distribution of plausible values. "Uncertainty" -- ratios and rankings are notoriously hard to get confidence intervals for in the frequentist world; here it falls out for free. "Risk of recommendation" is the row that matters most to breeders -- literally not computable without a posterior. Finish on the bottom bullets: mention Stan/NUTS only briefly (no need to teach HMC here), and flag that the prior hyperparameter is *data-driven*, not something the user has to tune -- we'll come back to the exact priors in a few slides.
</div>

## The core idea: defining "success"

Let $\Omega$ be the set of **high-performing genotypes**, sized by the **selection intensity** the breeder chooses (e.g., top 20%):

$$
\Pr(\hat{g}_j \in \Omega \mid y) = \frac{1}{S}\sum_{s=1}^{S} I\left(\hat{g}_j^{(s)} \in \Omega \mid y\right)
$$

```{mermaid}
%%| fig-width: 9.5
%%| fig-height: 2.4
%%| echo: false
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#dbe8f3", "primaryBorderColor": "#3b6ea5", "primaryTextColor": "#1a3a52", "lineColor": "#8a8a8a", "fontFamily": "Helvetica, Arial, sans-serif"}, "flowchart": {"padding": 22, "htmlLabels": true, "nodeSpacing": 35, "rankSpacing": 45, "wrappingWidth": 220}}%%
flowchart LR
    A["Fitted Bayesian<br/>model"]:::data --> B["S Monte Carlo<br/>draws"]:::data --> C["Define Ω:<br/>top performers"]:::amber --> D["Count successes<br/>inside Ω"]:::green --> E["Probability =<br/>successes / S"]:::green
    classDef data fill:#dbe8f3,stroke:#3b6ea5,stroke-width:2px,color:#1a3a52
    classDef amber fill:#fbe3c7,stroke:#b3611e,stroke-width:2px,color:#5c3413
    classDef green fill:#d9ede4,stroke:#1f6f54,stroke-width:2px,color:#123f30
```

In words: **simulate $S$ virtual trials** from the posterior, and count how often genotype $j$ would have been selected. The same counting logic — applied within an environment, to a pairwise comparison, or to a GEI variance — produces every other probability in the package.

<div class="notes">
[Point at the formula on screen while you explain it symbol by symbol.] "Omega is just 'the winners' circle' -- whatever top slice of genotypes the breeder wants, say top 20%. The hat-g-j is genotype j's estimated effect. This formula is really just counting." [Then point at the diagram, left to right.] Walk through the five boxes as a mechanical recipe: fit the model once, draw thousands of samples from its posterior, check in each sample whether genotype j would have made the cut, count the successes, divide by the total. That count-and-divide pattern is the engine behind literally everything else we'll see today -- performance, stability, pairwise comparisons, all of it.
</div>

## Four probability lenses

```{mermaid}
%%| fig-width: 9.5
%%| fig-height: 4
%%| echo: false
%%{init: {"theme": "base", "themeVariables": {"lineColor": "#8a8a8a", "fontFamily": "Helvetica, Arial, sans-serif"}, "flowchart": {"padding": 22, "htmlLabels": true, "nodeSpacing": 35, "rankSpacing": 45, "wrappingWidth": 220}}%%
flowchart TD
    Q1["Which genotypes perform<br/>best on average?"]:::qperf --> P1["Probability of<br/>superior performance"]:::aperf
    Q2["Is candidate A better<br/>than check B?"]:::qpair --> P2["Pairwise probability<br/>of performance"]:::apair
    Q3["Which genotypes are most<br/>consistent across sites?"]:::qstab --> P3["Probability of<br/>superior stability"]:::astab
    Q4["Which genotype is safest<br/>to recommend overall?"]:::qjoint --> P4["Joint probability<br/>(performance × stability)"]:::ajoint

    classDef qperf fill:#eef5fb,stroke:#3b6ea5,stroke-width:1.5px,color:#1a3a52
    classDef aperf fill:#dbe8f3,stroke:#3b6ea5,stroke-width:2px,color:#1a3a52
    classDef qpair fill:#eaf6f0,stroke:#2f8f6a,stroke-width:1.5px,color:#154430
    classDef apair fill:#d9ede4,stroke:#2f8f6a,stroke-width:2px,color:#154430
    classDef qstab fill:#fdf1e2,stroke:#b3611e,stroke-width:1.5px,color:#5c3413
    classDef astab fill:#fbe3c7,stroke:#b3611e,stroke-width:2px,color:#5c3413
    classDef qjoint fill:#f2eefa,stroke:#6a4c9c,stroke-width:1.5px,color:#33224d
    classDef ajoint fill:#ece3f5,stroke:#6a4c9c,stroke-width:2px,color:#33224d
```

Every one of these is a **direct, interpretable probability** — the same currency breeders already use when they talk about "risk." Stability is defined à la **Shukla (1972)**: low genotype-by-environment variance.

<div class="notes">
[Draw attention to the diagram -- give the audience a few seconds to read all four columns before narrating.] Frame this as a preview, not the full derivation: "These are the four questions ProbBreed answers, and later in the talk we'll write out the actual formula for each one." Walk left to right: raw performance, then a head-to-head comparison (useful for 'is my new line better than the check?'), then stability in the classic Shukla sense -- low GEI variance means the genotype behaves the same everywhere -- and finally the joint probability, which most breeders care about most because it rewards genotypes that are *both* good and dependable.
</div>

## The 3-step workflow

```{mermaid}
%%| fig-width: 9.5
%%| fig-height: 2.6
%%| echo: false
%%{init: {"theme": "base", "themeVariables": {"lineColor": "#8a8a8a", "fontFamily": "Helvetica, Arial, sans-serif"}, "flowchart": {"padding": 22, "htmlLabels": true, "nodeSpacing": 35, "rankSpacing": 45, "wrappingWidth": 220}}%%
flowchart LR
    D[("MET data")]:::data --> F1["bayes_met()<br/>fit the model"]:::green --> F2["extr_outs()<br/>extract + diagnose"]:::green --> F3["prob_sup()<br/>compute probabilities"]:::green --> R["Risk-aware<br/>recommendations"]:::amber
    classDef data fill:#dbe8f3,stroke:#3b6ea5,stroke-width:2px,color:#1a3a52
    classDef green fill:#d9ede4,stroke:#1f6f54,stroke-width:2px,color:#123f30
    classDef amber fill:#fbe3c7,stroke:#b3611e,stroke-width:2px,color:#5c3413
```

| Function | Role |
|---|---|
| `bayes_met()` | Fits a predefined Bayesian MET model via `rstan` |
| `extr_outs()` | Extracts posteriors, variance components, goodness-of-fit |
| `prob_sup()` | Computes every probability above, across and within environments |

<div class="notes">
This is the "zoom out" slide -- after all the theory, ground it in three function calls. [Trace the arrow left to right with your finger or the laser pointer.] "Everything you just saw collapses into three R functions: fit, extract, compute." Mention that this is deliberately the whole pipeline -- no other steps required -- which is the accessibility contribution of the package relative to the original Dias et al. method, which required writing Stan code by hand. We're about to spend the next several slides just on step one, `bayes_met()`, because that's where most of the modeling decisions live.
</div>

## Choosing a model: without year effect

::: columns
::: {.column width="46%"}
```r
mod = bayes_met(
  data = MyData,
  gen  = "Genotype",
  loc  = "Location",
  repl = ...,     # NULL / 'Replicate' /
                  # c('Replicate','Block')
  reg  = ...,     # NULL or 'Region'
  year = NULL,    # <- no year effect
  res.het = TRUE,
  trait = "Phenotype",
  iter = 2000, cores = 2, chains = 4)
```
:::

::: {.column width="4%"}
:::

::: {.column width="50%"}
```{=html}
<div class="model-scheme">
  <div class="scheme-branch">
    <div class="scheme-header header-neutral">reg = NULL</div>
    <div class="scheme-leaf leaf-green">Entry-mean<br><code>repl = NULL</code></div>
    <div class="scheme-leaf leaf-blue">RCBD<br><code>repl = 'Replicate'</code></div>
    <div class="scheme-leaf leaf-purple">IBD<br><code>repl = c('Repl','Block')</code></div>
  </div>
  <div class="scheme-branch">
    <div class="scheme-header header-amber">reg = 'Region'</div>
    <div class="scheme-leaf leaf-blue">RCBD<br><code>repl = 'Replicate'</code></div>
    <div class="scheme-leaf leaf-purple">IBD<br><code>repl = c('Repl','Block')</code></div>
  </div>
</div>
```
:::
:::

Five models live here: the entry-mean model **only** exists in this branch (`repl`, `reg`, and `year` all `NULL` together).

<div class="notes">
[Point at the code on the left first.] Walk through the generic call: gen, loc, and trait always required; repl/reg/year are the three switches. [Then point at the diagram on the right.] "When there's no year effect, bayes_met gives you five models: split first by whether you supply regions, then by your experimental design -- entry-mean if you only have adjusted means, RCBD if you have replicates, IBD if you have incomplete blocks." Stress the callout at the bottom: entry-mean is special, it's the *only* model where repl, reg, and year are all NULL simultaneously -- you can't combine entry-mean with regions or years.
</div>

## Choosing a model: with year effect

::: columns
::: {.column width="46%"}
```r
mod = bayes_met(
  data = MyData,
  gen  = "Genotype",
  loc  = "Location",
  repl = ...,     # 'Replicate' /
                  # c('Replicate','Block')
  reg  = ...,     # NULL or 'Region'
  year = "Year",  # <- year effect included
  res.het = TRUE,
  trait = "Phenotype",
  iter = 2000, cores = 2, chains = 4)
```
:::

::: {.column width="4%"}
:::

::: {.column width="50%"}
```{=html}
<div class="model-scheme">
  <div class="scheme-branch">
    <div class="scheme-header header-neutral">reg = NULL</div>
    <div class="scheme-leaf leaf-blue">RCBD<br><code>repl = 'Replicate'</code></div>
    <div class="scheme-leaf leaf-purple">IBD<br><code>repl = c('Repl','Block')</code></div>
  </div>
  <div class="scheme-branch">
    <div class="scheme-header header-amber">reg = 'Region'</div>
    <div class="scheme-leaf leaf-blue">RCBD<br><code>repl = 'Replicate'</code></div>
    <div class="scheme-leaf leaf-purple">IBD<br><code>repl = c('Repl','Block')</code></div>
  </div>
</div>
```
:::
:::

Four models live here — no entry-mean variant, since a year effect always implies replicated data.

<div class="notes">
Same structure as the previous slide, so you can move faster here -- [say "same idea, one switch flips"] and point at `year = "Year"` in the code. [Point at the diagram.] "With a year effect, entry-mean drops out entirely -- you always need RCBD or IBD, with or without regions -- four combinations total." If someone asks why: multi-year data structurally requires some notion of replication across time, which entry-mean's adjusted-means-only design doesn't support. Combined with the previous slide, that's all nine models bayes_met() can fit.
</div>

## Example: without year effect {.big-code}

```r
mod = bayes_met(
  data = MyData,
  gen = "Genotype",
  loc = "Location",
  repl = "Block",
  reg = "Region",
  year = NULL,
  res.het = FALSE,
  trait = "Phenotype",
  iter = 2000, cores = 2, chains = 4)
```

<div class="notes">
This slide is intentionally just one code block, full size, nothing else competing for attention. [Read the code aloud, line by line.] This is the RCBD-with-regions model: replicated data, no year information, and regions declared. Mention that this is taken directly from the package documentation, so it's exactly the syntax they'll find in `?bayes_met` and on the package website if they want to look it up later.
</div>

## Example: with year effect {.big-code}

```r
mod = bayes_met(
  data = MyData,
  gen = "Genotype",
  loc = "Location",
  repl = "Block",
  reg = "Region",
  year = 'Year',
  res.het = FALSE,
  trait = "Phenotype",
  iter = 2000, cores = 2, chains = 4)
```

<div class="notes">
[Compare side by side with the previous slide -- if possible, flip back one slide briefly to show the contrast.] Only one argument changed: `year = 'Year'` instead of `NULL`. Emphasize how little the user has to think about here -- the same five arguments (repl, reg, year, res.het, trait) fully determine which of the nine models gets fit, and the package handles the underlying Stan code either way.
</div>

## The full model catalog

Every combination is called the same way — just change `repl`, `reg`, `year` in `bayes_met()`:

```{=html}
<table class="model-catalog-table">
<thead>
<tr><th>Category</th><th>Variant</th><th>repl</th><th>reg</th><th>year</th></tr>
</thead>
<tbody>
<tr class="cat-mean"><td>Mean model</td><td>Entry-mean</td><td><code>NULL</code></td><td><code>NULL</code></td><td><code>NULL</code></td></tr>
<tr class="cat-rcbd"><td>RCBD</td><td>Only locations</td><td><code>'Replicate'</code></td><td><code>NULL</code></td><td><code>NULL</code></td></tr>
<tr class="cat-rcbd"><td>RCBD</td><td>+ Regions</td><td><code>'Replicate'</code></td><td><code>'Region'</code></td><td><code>NULL</code></td></tr>
<tr class="cat-rcbd"><td>RCBD</td><td>+ Years</td><td><code>'Replicate'</code></td><td><code>NULL</code></td><td><code>'Year'</code></td></tr>
<tr class="cat-rcbd"><td>RCBD</td><td>+ Regions &amp; Years</td><td><code>'Replicate'</code></td><td><code>'Region'</code></td><td><code>'Year'</code></td></tr>
<tr class="cat-ibd"><td>Incomplete Blocks</td><td>Only locations</td><td><code>c('Replicate','Block')</code></td><td><code>NULL</code></td><td><code>NULL</code></td></tr>
<tr class="cat-ibd"><td>Incomplete Blocks</td><td>+ Regions</td><td><code>c('Replicate','Block')</code></td><td><code>'Region'</code></td><td><code>NULL</code></td></tr>
<tr class="cat-ibd"><td>Incomplete Blocks</td><td>+ Years</td><td><code>c('Replicate','Block')</code></td><td><code>NULL</code></td><td><code>'Year'</code></td></tr>
<tr class="cat-ibd"><td>Incomplete Blocks</td><td>+ Regions &amp; Years</td><td><code>c('Replicate','Block')</code></td><td><code>'Region'</code></td><td><code>'Year'</code></td></tr>
</tbody>
</table>
```

- `res.het` toggles homogeneous vs. heterogeneous residual variance across locations
- Full equations for all nine: `?bayes_met` or the package site, Section 2

<div class="notes">
This slide is the reference card -- [say "you don't need to memorize this, it's here so you know where to look it up later"]. Point out the three color bands: green for the single mean model, blue for the four RCBD variants, purple for the four incomplete-block variants -- same colors we've used in every diagram so far, so the mapping should already feel familiar. If someone asks "what if I have a design that doesn't fit any of these nine", mention this is exactly what the res.het, iter, chains, and the `...` passthrough to rstan::sampling() are for -- there's room to customize beyond the nine presets.
</div>

## Our working example: the `maize` dataset

::: {.incremental}
- **36 genotypes**: 32 experimental hybrids + 4 commercial checks
- **16 locations** across **5 regions** (mega-environments)
- **Incomplete block design**: block size 6, 2 replicates — data from Dias *et al.* (2022), courtesy of Embrapa Milho e Sorgo
- Trait: grain yield (`GY`)
:::

::: {.fragment}
```{mermaid}
%%| fig-width: 9.5
%%| fig-height: 2.2
%%| echo: false
%%{init: {"theme": "base", "themeVariables": {"lineColor": "#8a8a8a", "fontFamily": "Helvetica, Arial, sans-serif"}, "flowchart": {"padding": 22, "htmlLabels": true, "nodeSpacing": 35, "rankSpacing": 45, "wrappingWidth": 220}}%%
flowchart LR
    A["Load maize data"]:::data --> B["Fit IBD + Region<br/>model: bayes_met()"]:::green --> C["Diagnose fit:<br/>extr_outs()"]:::green --> D["Compute probabilities:<br/>prob_sup()"]:::green --> E["Rank genotypes for<br/>recommendation"]:::amber
    classDef data fill:#dbe8f3,stroke:#3b6ea5,stroke-width:2px,color:#1a3a52
    classDef green fill:#d9ede4,stroke:#1f6f54,stroke-width:2px,color:#123f30
    classDef amber fill:#fbe3c7,stroke:#b3611e,stroke-width:2px,color:#5c3413
```
:::

::: {.fragment}
Please have `R`, `ProbBreed`, and a working `rstan` toolchain ready — we switch to the tutorial next.
:::

<div class="notes">
[Click through the bullets, give each a beat.] Introduce the dataset we'll actually run: real maize trial data from the original Dias et al. 2022 paper, provided by Embrapa Milho e Sorgo -- so this isn't a toy example, it's the dataset that motivated the method in the first place. Point out that this maps onto the IBD-with-regions box from two slides ago -- 16 locations, 5 regions, incomplete blocks, 2 reps. [Ask everyone to open their laptop now] and confirm ProbBreed and rstan are installed before moving on, since the next section of the talk (priors and formulas) sets up the theory we're about to see in the tutorial's real output.
</div>

## Model priors

Priors follow **Dias *et al.* (2022)** — weakly informative, but never fully uninformative:

$$
x \sim N\left(0,\, S^{[x]}\right), \qquad \sigma \sim \text{HalfCauchy}\left(0,\, S^{[\sigma]}\right)
$$

where $x$ is any model effect except the residual, and $\sigma$ is the residual standard deviation. For heterogeneous-variance models (`res.het = TRUE`):

$$
\sigma_k \sim \text{HalfCauchy}\left(0,\, S^{[\sigma_k]}\right)
$$

The hyperprior on every scale parameter is itself data-driven:

$$
S^{[x]} \sim \text{HalfCauchy}(0,\, \phi), \qquad \phi = \max(y) \times 10
$$

<div class="notes">
This is the "what's actually happening inside bayes_met()" slide -- take it a bit slower since it's the densest math of the talk. [Point at the first formula.] Every fixed and random effect gets a Normal(0, S) prior; every variance/SD term gets a Half-Cauchy, which is standard practice because it keeps variances non-negative while still being weakly informative. [Point at the second formula.] When residual variance is allowed to differ by location -- res.het = TRUE -- each location gets its own sigma_k with its own Half-Cauchy prior. [Point at the third formula, the most important one on this slide.] The key design choice is that the hyperparameter phi isn't a fixed number the user has to guess -- it scales with the data itself, ten times the maximum observed phenotype, which keeps the prior vague enough to let the data dominate while still bounding it away from being improper.
</div>

## Probability of superior performance

Across environments — genotype $j$'s marginal effect:

$$
\Pr(\hat{g}_j \in \Omega \mid y) = \frac{1}{S}\sum_{s=1}^{S} I\left(\hat{g}_j^{(s)} \in \Omega \mid y\right)
$$

Pairwise — head-to-head against a specific competitor or check cultivar:

$$
\Pr(\hat{g}_j > \hat{g}_{j'} \mid y) = \frac{1}{S}\sum_{s=1}^{S} I\left(\hat{g}_j^{(s)} > \hat{g}_{j'}^{(s)} \mid y\right)
$$

*(flip the inequality to $<$ when selecting for a decreasing trait, e.g. plant height or disease severity)*

<div class="notes">
[Point at the formula on the left -- the first equation.] This is the formal version of the counting-game diagram from earlier: same S posterior draws, same indicator function, same division by S. [Point at the second formula.] The pairwise version answers a much more concrete breeding question -- "is my elite candidate actually better than the commercial check?" -- by comparing the two genotypes' draws directly in every posterior sample, rather than each against the whole population. Flag the footnote about flipping the inequality; it's a one-argument change in the R function (`increase = FALSE`) but easy to forget conceptually.
</div>

## Probability of superior stability

A genotype is "stable" (Shukla, 1972) when its genotype-by-environment interaction variance is **low**:

$$
\Pr\left[\text{var}(\widehat{ge}_{jk}) \in \Omega \mid y\right] = \frac{1}{S}\sum_{s=1}^{S} I\left[\text{var}\left(\widehat{ge}_{jk}^{(s)}\right) \in \Omega \mid y\right]
$$

Pairwise stability — which of two genotypes is more consistent:

$$
\Pr\left[\text{var}(\widehat{ge}_{jk}) < \text{var}(\widehat{ge}_{j'k}) \mid y\right] = \frac{1}{S}\sum_{s=1}^{S} I\left[\text{var}\left(\widehat{ge}_{jk}^{(s)}\right) < \text{var}\left(\widehat{ge}_{j'k}^{(s)}\right) \mid y\right]
$$

<div class="notes">
[Point at the stability definition at the top before moving to the formula.] Ground this before the math: stability here is *not* about high performance, it's about the genotype behaving the same way everywhere -- low genotype-by-environment interaction variance. Same counting recipe as performance, just applied to the variance component instead of the main effect -- [point at var(ge_jk) in the formula] note that "belonging to Omega" here means having a variance small enough to be in the low-variance group, not a high value. The pairwise version at the bottom directly compares two genotypes' consistency, which is often what breeders actually want to know: "is line A more reliable than line B across our target region?"
</div>

## Joint probability of performance and stability

Assuming genotypic main effects and GEI variances are independent by model design:

$$
\Pr\left[\hat{g}_j \in \Omega \,\cap\, \text{var}(\widehat{ge}_{jk}) \in \Omega\right] = \Pr(\hat{g}_j \in \Omega) \times \Pr\left[\text{var}(\widehat{ge}_{jk}) \in \Omega\right]
$$

::: {.incremental}
- Simply the **product** of the two probabilities from the previous slide
- Rewards genotypes that are simultaneously **high-performing and dependable**
- Rarely will the same genotype top every individual metric — this is the one that reconciles them
:::

<div class="notes">
[Point at the formula -- it's just a multiplication, make that clear right away.] "This looks intimidating but it's literally just performance-probability times stability-probability." Because the model treats the genotypic main effect and the GxE variance as independent random quantities, the joint probability of two independent events is just their product -- no new machinery needed. [Click through the bullets one at a time.] This is usually the number breeders care about most in a final recommendation decision, precisely because it's rare for the same genotype to win on every single metric -- this is the tie-breaker.
</div>

## Within-environment results

Same logic, applied **locally** instead of across the whole trial network — useful for site-specific recommendations:

$$
\Pr(\hat{g}_{jk} \in \Omega_k \mid y) = \frac{1}{S}\sum_{s=1}^{S} I\left(\hat{g}_{jk}^{(s)} \in \Omega_k \mid y\right)
$$

$$
\Pr(\hat{g}_{jk} > \hat{g}_{j'k} \mid y) = \frac{1}{S}\sum_{s=1}^{S} I\left(\hat{g}_{jk}^{(s)} > \hat{g}_{j'k}^{(s)} \mid y\right)
$$

Both are computed **per environment** — one heatmap per location or region, not a single global number.

<div class="notes">
[Point at the subscript k in the formulas -- that's the only difference from the "across" versions.] Everything on this slide is structurally identical to the across-environment formulas two slides ago, with one change: the genotypic effect now includes the environment-specific deviation (g-hat-jk instead of g-hat-j), and Omega-k is the top slice *within* that one environment. Explain why this matters practically: a breeder recommending cultivars to growers in one specific region doesn't care about performance averaged across the whole trial network -- they care about performance right there. In the tutorial, this is what produces the heatmaps rather than the lollipop charts.
</div>

## Key takeaways

::: {.incremental}
1. Cultivar recommendation is a decision under **uncertainty** — GEI makes single-estimate rankings incomplete
2. `ProbBreed` recasts that uncertainty as directly interpretable **probabilities**, from a Bayesian MET model's posterior
3. One consistent input — the **selection intensity** — drives performance, stability, pairwise, and joint probabilities
4. Three functions: `bayes_met()` → `extr_outs()` → `prob_sup()`
:::

::: {.fragment}
**Next:** continue with `hands_on_tutorial.qmd` — we fit `bayes_met()` on the `maize` dataset together.
:::

<div class="notes">
[Click through the items one at a time, pausing on each.] This is the summary slide -- resist the urge to re-explain everything, just land each point once. Close by pointing everyone to the tutorial file and confirming they have R and ProbBreed ready, since that's the very next thing that happens after this slide.
</div>

## References

Chaves, S. F. S., Krause, M. D., Dias, L. A. S., Garcia, A. A. F., & Dias, K. O. G. (2024). ProbBreed: a novel tool for calculating the risk of cultivar recommendation in multienvironment trials. *G3: Genes\|Genomes\|Genetics*, 14(3), jkae013. <https://doi.org/10.1093/g3journal/jkae013>

Dias, K. O. G., Santos, J. P. R., Krause, M. D., Piepho, H.-P., Guimarães, L. J. M., Pastina, M. M., & Garcia, A. A. F. (2022). Leveraging probability concepts for cultivar recommendation in multi-environment trials. *Theoretical and Applied Genetics*, 135(4), 1385–1399. <https://doi.org/10.1007/s00122-022-04041-y>

Package documentation: <https://saulo-chaves.github.io/ProbBreed_site/>

<div class="notes">
Leave this slide up for a few seconds in silence so people can photograph it or note the DOIs down -- no need to read the citations aloud word for word. Mention that both papers, plus a link to the package site, are also listed at the top of the hands-on tutorial document for anyone who wants them later.
</div>

## {.center-slide}

::: {.r-fit-text}
Thank you!<br>Questions welcome.
:::

<div class="notes">
Open the floor. If the room is quiet, prime it with one concrete question of your own, e.g. "has anyone here already used AMMI or GGE biplots for a dataset like this? Curious how a probability-based ranking would have compared."
</div>
